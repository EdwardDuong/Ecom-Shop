version: '3.8'
services:
  ecom-shop-api:
    environment:
      - MONGO=mongodb+srv://edwardduong:anhden11@ecom-shop.pqzstr4.mongodb.net/?retryWrites=true&w=majority
    image:
      edwardduong/ecom-shop-api
    volumes:
      - ./:/usr/app
    container_name: ecom-shop-api
    ports:
      - 5000:5000
    command: node index.js
    networks:
      - mongodb_network

  ecom-shop-client:
    image:
      edwardduong/ecom-shop-client
    container_name: ecom-shop-client
    network_mode: host
    ports:
      - 80:80
    depends_on:
      - ecom-shop-api

volumes:
  mongodb:

networks:
  mongodb_network:
    name: mongodb_network